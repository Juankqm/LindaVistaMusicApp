<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.lindavista</groupId>
    <artifactId>LindaVistaMusicPlayer</artifactId>
    <version>1.0-SNAPSHOT</version>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <maven.compiler.source>21</maven.compiler.source>
        <maven.compiler.target>21</maven.compiler.target>
        <javafx.version>21.0.8</javafx.version>
    </properties>

    <dependencies>
        <!-- JavaFX -->
        <dependency>
            <groupId>org.openjfx</groupId>
            <artifactId>javafx-controls</artifactId>
            <version>${javafx.version}</version>
        </dependency>
        <dependency>
            <groupId>org.openjfx</groupId>
            <artifactId>javafx-fxml</artifactId>
            <version>${javafx.version}</version>
        </dependency>
        <dependency>
            <groupId>org.openjfx</groupId>
            <artifactId>javafx-media</artifactId>
            <version>${javafx.version}</version>
        </dependency>

        <!-- JLayer for MP3 -->
        <dependency>
            <groupId>javazoom</groupId>
            <artifactId>jlayer</artifactId>
            <version>1.0.1</version>
        </dependency>

        <!-- ControlsFX -->
        <dependency>
            <groupId>org.controlsfx</groupId>
            <artifactId>controlsfx</artifactId>
            <version>11.1.2</version>
        </dependency>

        <!-- BootstrapFX -->
        <dependency>
            <groupId>org.kordamp.bootstrapfx</groupId>
            <artifactId>bootstrapfx-core</artifactId>
            <version>0.4.0</version>
        </dependency>
    </dependencies>

    <build>

  <resources>
    <resource>
      <directory>src/main/resources</directory>
      <includes>
        <include>**/*.fxml</include>
        <include>**/*.css</include>
        <include>**/*.png</include>
        <include>**/*.svg</include>
      </includes>
    </resource>
  </resources>


        <plugins>
            <!-- Shade Plugin para JAR ejecutable -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-shade-plugin</artifactId>
                <version>3.4.1</version>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals>
                            <goal>shade</goal>
                        </goals>
                        <configuration>
                            <shadedArtifactAttached>true</shadedArtifactAttached>
                            <shadedClassifierName>shaded</shadedClassifierName>
                            <transformers>
                                <transformer implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
                                    <mainClass>com.lindavista.lindavistamusicplayer.MainApp</mainClass>
                                </transformer>
                            </transformers>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <!-- jpackage Maven Plugin -->
            <plugin>
                <groupId>org.panteleyev</groupId>
                <artifactId>jpackage-maven-plugin</artifactId>
                <version>1.7.0</version>
                <executions>
                    <execution>
                        <id>package-windows</id>
                        <goals>
                            <goal>jpackage</goal>
                        </goals>
                        <configuration>
                            <type>exe</type> <!-- Genera EXE para Windows -->
                            <name>LindaVistaMusicPlayer</name>
                            <appVersion>1.0</appVersion>
                            <vendor>LindaVista</vendor>
                            <input>${project.build.directory}</input>
                            <mainJar>LindaVistaMusicPlayer-1.0-SNAPSHOT-shaded.jar</mainJar>
                            <mainClass>com.lindavista.lindavistamusicplayer.MainApp</mainClass>
                            <modulePaths>
                                <modulePath>C:\Users\Juan\Desktop\openjfx-21.0.8_windows-x64_bin-sdk\javafx-sdk-21.0.8\lib</modulePath>
                            </modulePaths>
                            <javaOptions>
                                <javaOption>--add-modules javafx.controls,javafx.fxml,javafx.media</javaOption>
                            </javaOptions>
                            <icon>src/main/resources/icons/logo-app.ico</icon>
                            <resourceDir>${project.basedir}/MySongs</resourceDir>
                            <winDirChooser>true</winDirChooser>
                            <winMenu>true</winMenu>
                            <winShortcut>true</winShortcut>
                            <createRuntimeImage>true</createRuntimeImage> <!-- Incluye Java automáticamente -->
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
